# Gallery read model

Small, source-neutral product read-model logic shared by the local Prototype Gallery and the formal shadow reader. It contains no persistence adapter, HTTP endpoint, database access, or source collection code.
