# Infrastructure examples

This directory is reserved for non-secret, non-production configuration examples. Examples must never contain real credentials, production endpoints, databases, object data, or deployment automation.
