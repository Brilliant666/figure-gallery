# Codex Master Instruction

本文件规定本仓库中所有研究、验证、设计和实现任务必须遵守的治理规则。

## 分支与交付

- 所有工作必须使用任务独立分支，不得直接在默认分支上开展任务。
- 默认创建 Draft PR。
- 未经用户明确要求，不得合并 PR。
- 未经用户明确要求，不得部署任何应用、服务或资源。
- 每轮工作达到当前任务的停止条件后必须立即停止，不得擅自开始下一阶段或扩大任务范围。

## 研究与证据

- 所有调研结论必须提供可核验的证据和来源。
- 不得将推测、假设或未经验证的信息写成事实；不确定内容必须明确标注。
- `research/` 只用于保存研究结论、分析和相关说明。
- `research/evidence/` 只用于保存支撑结论的小型、必要且已脱敏的证据。
- 不得提交大型网页存档、批量抓取结果、敏感信息或与结论无关的原始数据。

## 凭据与外部访问

- 不得使用账号 Cookie、私人 Token、密码、会话信息或其他凭据。
- 不得请求、记录、提交或泄露任何凭据。
- 不得绕过验证码、登录限制、访问控制、付费限制或反自动化机制。
- 对外部网站的访问必须是有限、低频、只读且与当前任务直接相关的。
- 如果网站明确拒绝自动化访问或需要绕过限制，应立即停止并记录限制，不得规避。

## 技术验证与正式项目限制

- `spikes/` 只用于保存可丢弃的技术验证，不得将其中内容视为正式产品实现。
- 技术栈已经接受为 Payload CMS `3.86.x`、Next.js `16.2.x`、React `19.2.x`、TypeScript 和 Node.js `22.x`，正式数据与媒体分别使用 PostgreSQL 16 和 S3 兼容对象存储。
- `x` 只表示接受的兼容线；当前已验证精确基线为 Payload `3.86.0`、Next.js `16.2.10`、React `19.2.7`。正式项目必须锁定精确版本，任何不同补丁都须经依赖升级和生产门禁，不得自动升级。
- 正式应用只能在明确授权的 PR-00 中，从 Payload 官方脚手架于 `apps/web` 干净生成；禁止复制、移动、导入或改造 `spikes/` 代码作为正式实现。
- `spikes/` 永远不得进入正式应用依赖图；`research/` 只保存历史研究证据，不得成为运行时输入。
- 技术验证不得自行改变已接受的技术决策；需要重新评估时必须建立独立任务和 ADR。
- 未经当前任务明确要求，不得开始正式产品开发、基础设施配置或部署工作。

## 正式交付路线

- 正式变化必须使用任务独立分支和独立 PR，按 `docs/DELIVERY_ROADMAP.md` 的 PR-00—PR-08 顺序推进。
- PR-00 只允许建立官方脚手架、依赖锁定、环境变量 schema、health、空 migration、CI、测试框架和模块边界，不得实现完整业务功能。
- 任一 PR 达到停止条件后必须停止；未经新任务明确授权，不得自动开始后续 PR。
- 已验证的 spike 配置、workflow 或 standalone 输出不能直接视为正式应用配置，正式初始化后必须重新建立 CI 和部署门禁。

## Hpoi 与来源边界

- Hpoi 当前只允许作为人工参考；禁止自动访问 Hpoi 及其任何子域名。
- 未取得明确书面许可前，不得编写、测试或运行正式 Hpoi adapter。
- 第一阶段来源只可人工录入、人工粘贴 URL、从明确允许的离线文件导入，或由未来经授权的 Source Adapter 接入。
- 所有来源数据只能写入候选聚合，必须经过人工审核；不得直接创建、覆盖或删除正式数据与正式主图。

## 范围控制

- 每次任务只执行任务明确列出的工作。
- 当前任务未授权的调研、设计、技术选型、实现、合并和部署均不得开展。
- 遇到超出授权范围、需要凭据或需要规避访问限制的情况，应停止并报告。
